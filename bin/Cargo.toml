[package]
name = "bin"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# atb = { git = "https://github.com/aetheras-io/atb-rs", tag = "v0.12.6", features = ["http", "graphql", "sql", "jwt", "fixtures", "eventsourcing", "serde_utils"] }
actix-web = "4"
actix-cors = "0.6.1"
structopt = "0.3.20"
serde = { version = "1.0.114", features = ["derive"] }
serde_json = "1.0.57"
chrono = "0.4"
log = "0.4"
reqwest = { version = "0.11.10", features = ["blocking", "json"] }